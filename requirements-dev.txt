# Инструменты разработки. Основные зависимости — в requirements.txt.
# Версия закреплена точно: у ruff новые правила приезжают с минорными релизами,
# и незакреплённый диапазон делает CI красным без единого изменения в коде.
ruff==0.16.4
coverage>=7.6,<8.0
